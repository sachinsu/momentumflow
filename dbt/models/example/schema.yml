
version: 2

models:
    - name: momentumstocks
      description: "model to get top 20 momentum stocks"
      columns:
          - name: symbol
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
